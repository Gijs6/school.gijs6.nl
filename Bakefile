#!/bin/bash

build() {
  .venv/bin/python binder.py
}

format() {
  ruff format .
  prettier --write "**/*.{css,js}"
  prettier --write "**/*.{json,xml,yml}"
  djlint . --reformat
  markdownlint --fix "**/*.md" || true
}
