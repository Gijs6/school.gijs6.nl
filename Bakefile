#!/bin/bash

serve() {
  .venv/bin/python binder.py serve
}

build() {
  .venv/bin/python binder.py build
}

deploy() {
  build
  lift deploy
}

format() {
  ruff format .
  prettier --write "**/*.{css,js}"
  prettier --write "**/*.{json,xml}"
  djlint . --reformat
}
